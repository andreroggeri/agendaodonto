apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: messaging-redis-volume-claim
  labels:
    app: messaging
    env: staging
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
